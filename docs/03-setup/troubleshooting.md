# Troubleshooting
- **CORS Errors**: Ensure `VITE_API_BASE_URL` matches the backend address and backend `SecurityConfig` allows the origin.
- **DB Connection Refused**: Check PostgreSQL service and credentials in `application.properties`.
- **JWT Expired**: Log in again to get a new token.
