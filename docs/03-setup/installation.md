# Installation
1. Clone the repository.
2. Create PostgreSQL database `prepmate`.
3. Configure `application.properties` in backend.
4. Configure `.env.local` in frontend.
5. Run Backend: `./mvnw spring-boot:run`
6. Run Frontend: `npm run dev`
