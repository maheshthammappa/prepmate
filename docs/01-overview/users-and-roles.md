# Users and Roles
Currently, the system implements a single user role.
- **User**: Can register, log in, take interviews, view history, and practice.

> **Implementation Status:** Admin roles are `Planned` / `Not Implemented`.
